apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: kube101-helm-fee-router
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/godzillaba/argocd-testing
        revision: use-helm
        directories:
          - path: helm/fee-router/instances/*
  template:
    metadata:
      name: 'helm-fee-router-{{path.basename}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/godzillaba/argocd-testing
        targetRevision: use-helm
        path: 'helm/fee-router/chart'
        helm:
          valueFiles:
            - '../instances/{{path.basename}}/values.yaml'
      destination:
        server: https://kubernetes.default.svc
        namespace: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
